"""
Background worker entry point.

This is a placeholder for future arq-based background tasks.
Workers will use shared/ for models and DB access.
"""

# Future: from arq import create_pool
# Future: from arq.connections import RedisSettings


class WorkerSettings:
    """arq WorkerSettings placeholder."""

    # Future: redis_settings = RedisSettings.from_dsn(settings.REDIS_URL)
    # Future: functions = [...]
    pass
